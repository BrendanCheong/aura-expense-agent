/**
 * Appwrite row types — re-exported from the CLI-generated appwrite.d.ts
 * with domain-friendly aliases.
 *
 * These types match the actual SDK responses (snake_case field names).
 * Regenerate the source types via:
 *   appwrite types src/types/appwrite --language ts
 */

export type {
  Users as UserRow,
  Categories as CategoryRow,
  Transactions as TransactionRow,
  Budgets as BudgetRow,
  VendorCache as VendorCacheRow,
} from './appwrite';
